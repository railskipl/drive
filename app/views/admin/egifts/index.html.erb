<table>
<tr>
    <th>Name:</th>
</tr>

<% @egifts.each do |egift|%>
  <tr>
    <td><%= egift.credit %></td>
    <td><%= image_tag egift.image.url %></td>
    <td><%= link_to 'Edit', edit_admin_egift_path(egift) %></td>
    <td><%= link_to 'Destroy',admin_egift_path(egift) ,:method => :delete , data: { confirm: 'Are you sure?' } %>
    </td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Car Model', new_admin_egift_path %>
