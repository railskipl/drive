<%= form_for(@user_blog) do |f| %>
   <% if @user_blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_blog.errors.count, "error") %> prohibited this car make from being saved:</h2>

      <ul>
      <% @user_blog.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label "Blog Category" %><br />
    <%= f.collection_select :blog_id, Blog.all ,:id,:name, :prompt => 'Select Blog'%>
  </div> 

  <div class="field">
    <%= f.label "Blog Description" %><br />
    <%= f.text_area :body ,:rows=>10, :cols=>200,:style=> "width: 600px;"%>
  </div>

  <%= f.hidden_field :user_id,:value => current_user.id %>
  
  
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>