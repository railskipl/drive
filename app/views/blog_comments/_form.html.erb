
<%= form_for(@blog_comment) do |f| %>
  <% if @blog_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog_comment.errors.count, "error") %> prohibited this blog_comment from being saved:</h2>

      <ul>
      <% @blog_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <textarea class="comment_tf2" rows="4" name="blog_comment[body]"></textarea>
  </div>
  <%= f.hidden_field :user_blog_id %>
  <%= f.hidden_field :user_id,:value => current_user.id %>
  <div style="float:right; margin:10px 0; text-align:left; width:100%">
          <input type="submit" value="Done" style="margin-left:0px" class="submit_btn" name="button">
  </div>
<% end %>
