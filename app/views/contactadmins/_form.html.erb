<%= form_for(@contactadmin) do |f| %>
  <% if @contactadmin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contactadmin.errors.count, "error") %> prohibited this contactadmin from being saved:</h2>

      <ul>
      <% @contactadmin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message %>
  </div>
  <div class="actions">
    <%= f.submit "Publish News or Announcement"%>
  </div>
<% end %>
